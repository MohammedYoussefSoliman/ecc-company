"use strict";


// hero carsoul

$('.owl-carousel').owlCarousel({
    items:1,
    loop:true,
    autoplay:true,
    autoplayTimeout:5000,
    animateIn: 'fadeIn',
    animateOut: 'fadeOut',
    smartSpeed:3000
});